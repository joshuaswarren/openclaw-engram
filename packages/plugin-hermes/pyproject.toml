[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "engram-hermes"
version = "0.1.0"
description = "Engram MemoryProvider plugin for Hermes Agent"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Joshua Warren" }]
keywords = ["engram", "memory", "hermes", "ai-agent", "memory-provider"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "httpx>=0.25.0",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
]

[project.urls]
Homepage = "https://github.com/joshuaswarren/openclaw-engram"
Repository = "https://github.com/joshuaswarren/openclaw-engram"
Issues = "https://github.com/joshuaswarren/openclaw-engram/issues"

[tool.hatch.build.targets.wheel]
packages = ["engram_hermes"]

[tool.mypy]
strict = true
python_version = "3.10"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
