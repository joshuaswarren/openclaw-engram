#!/usr/bin/env tsx
/**
 * engram CLI binary entry point.
 *
 * main() is auto-invoked by index.ts when this file is executed directly
 * (detected via process.argv[1] matching engram.ts/engram.js).
 * Do NOT call main() here — doing so causes double-execution.
 */
import "../src/index.ts";
