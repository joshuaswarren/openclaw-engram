{
  "name": "@engram/server",
  "version": "0.1.0",
  "description": "Standalone Engram memory server — HTTP + MCP without OpenClaw",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "engram-server": "./dist/bin/engram-server.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --target es2022 --platform node --outDir dist && tsup bin/engram-server.ts --format esm --target es2022 --platform node --outDir dist/bin",
    "check-types": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@engram/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/joshuaswarren/openclaw-engram.git",
    "directory": "packages/engram-server"
  }
}
