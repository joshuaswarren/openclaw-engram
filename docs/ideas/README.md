# Ideas Backlog

This directory holds future-facing ideas that are not current plugin roadmap plans.

Use `docs/plans/` for implementation-ready, plugin-feasible work.
Use `docs/ideas/` for concepts blocked on external dependencies or waiting on OpenClaw core capabilities.
